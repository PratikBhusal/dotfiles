" Yank from system clipboard
set clipboard+=unnamed

" Shortcut to get into command mode. I don't really use the default normal mode
" binding for ';'
nnoremap ; :

" Do incremental searching.
set incsearch

set number
set relativenumber

" Make searching less tedious
set ignorecase smartcase

" source ~/.config/vim/vimrc
